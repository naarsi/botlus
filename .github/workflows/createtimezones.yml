name: createTimeZones

on:
  schedule:
    - cron: '*/20 * * * *'

jobs:
  call-api:
    runs-on: ubuntu-latest
    
    steps:
      - name: Make cURL request
        run: |
          curl -X GET https://timeconverter-d3a3.onrender.com/timeframe/createtimezones
